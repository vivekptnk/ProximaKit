# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# Xcode (ignore generated projects except the demo app)
xcuserdata/
DerivedData/
*.xcresult
*.xcworkspace

# macOS
.DS_Store
**/.DS_Store

# IDE
*.swp
*~
.vscode/
.idea/

# ML Models (too large for git)
Models/

# Python
__pycache__/
*.pyc
venv/
.venv/

# Claude Code (internal dev tooling)
.claude/

# Internal planning & docs (kept locally, not public)
docs/PRD.md
docs/MODEL_GUIDE.md
CLAUDE.md
scripts/

# Benchmark artifacts — datasets and harness output, NOT the harness itself.
# The Benchmarks/ source tree is tracked (see Benchmarks/README.md); these
# subpaths are generated on demand and too large for git.
Benchmarks/datasets/sift-1m/
Benchmarks/datasets/ms-marco/
Benchmarks/datasets/*.tar.gz
Benchmarks/out/
